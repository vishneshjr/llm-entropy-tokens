[project]
name = "llm-entropy-tokens"
version = "0.1.0"
description = "LLM Entropy Tokens"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "accelerate>=1.13.0",
    "datasets>=3.0.0",
    "torch>=2.11.0",
    "transformers>=5.5.0",
]
